Perform following SQL queries on the database created in assignment 4.  
* Implementation of relational operators in SQL  
* Boolean operators and pattern matching  
* Arithmetic operations and built in functions  
* Group functions  
* Processing Date and Time functions  
* Complex queries and set operators 


show databases; 
use seit26; 
select * from emp where job like 'manager'; 
select * from emp where comm is null; 
select * from emp where sal between 1000 and 3000; 
select * from emp where ename like '%r'; 
select * from emp order by sal desc; 
select min(sal) from emp where deptno=20; 
select sum(sal),avg(sal),min(sal),max(sal),count(sal) from emp; 
select deptno,sum(sal) from emp group by deptno; 
select empno,ename,dept.deptno,dname,loc from emp,dept; 
select empno,ename,dept.deptno,dname,loc from emp,dept where 
emp.deptno=dept.deptno; 
select empno,ename,dept.deptno,dname,loc from emp right outer join dept on 
emp.deptno=dept.deptno; 
select e.empno,e.ename,m.empno,m.ename from emp e,emp m where e.mgr=m.empno; 
select job from emp where deptno=10; 
select job from emp where deptno=20; 
select job from emp where deptno=10 union select job from emp where deptno=20; 
select job from emp where deptno=10 intersect select job from emp where deptno=20; 
select * from emp where deptno=(select deptno from emp where ename like 'miller' limit 
1); 
select * from emp where sal>all(select sal from emp where deptno=20); 
